#
# .bashrc.$(hostname -s)
#

logPrint "Processing ~/.bashrc.$(hostname -s)"

alias zzz="apt-get upgrade && apt-get update"

export PODS=/var/lib/
export ZWJS="/var/lib/zwave"
export LANG=POSIX
export SSH_AGENT_PID="Not now"
export ZIGBEE="/var/lib/zigbee2mqtt"
