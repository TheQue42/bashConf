#
# .bashrc.$(hostname -s)
#

logPrint "Processing ~/.bashrc.$(hostname -s)"

alias zzz="apt-get upgrade && apt-get update"

export DOCKER="docker"
#export CRI_SOCKET=/run/podman/podman.sock
export PODS=/pods
export ZWJS="$PODS/zwave"
export ZIGBEE="$PODS/z2m"
export SSH_AGENT_PID="Not now"
export LANG=POSIX


